# Session record — 2026-07-06 (the day after FINAL_REPORT)

One session, all on branch `claude/magnitude-trading-framework-nt7h0q` (PR #1), all
runs on the local data machine (T7 mounted; full 2016-06→2026-06 dataset, 755G
outputs + 51G bars_1m_raw), total incremental cost **$0**.

## What happened, in order

1. **PR #1 reviewed** (8-angle review, findings verified empirically against the
   parquet files). 8 findings in `scripts/phase7_forecast_value.py` v1, including a
   startup crash, two false-PASS biases (dollar-ATR baseline, calendar-day embargo
   leak), and the discovery that `ret_1d` is an overlapping entry(10:00)→next-close
   return. Docs misquoted +0.0076 as the lift over persistence (truth: +0.036;
   +0.0076 is over informed).
2. **Amendment v2** (commit `f102eea`, pre-run): all fixes applied, pre-registration
   amended with every deviation enumerated; `--smoke` mode added (2017 pseudo-OOS) so
   engineering tests never unblind 2018-2020.
3. **Test 7.1 EXECUTED — NULL on all three arms** (`a6a27ee`, 164s):
   `phase7-findings.md` §7.1. Arm A mechanical null (ML RMSE 0.1544 loses to RV21
   0.1446 and BLEND √(RV·VIX) 0.1255); Arm B dead even (ΔSharpe CI [−0.02,+0.04]);
   Arm C Δharvest +0.04%/yr CI incl. 0. Channel G closed at the margin; naive
   trailing vol is the standing risk input; 2021-22 stays sealed.
4. **Phase 8 frozen and EXECUTED — short interest, 0/6 NULL** (`20c585e`, `87ce4e7`):
   `phase8-findings.md`. Gate answerable on mean-IC only; ΔSI/5d survived FDR
   (−0.019, p=0.002) but sign-flipped in the 2020 squeeze era → NULL by the frozen
   3/3 rule. Positioning data (the phase1 X.1 gap) now has a measured answer.
5. **Phase 9 — Feature Dynamics Atlas** (`b26e5c8`): `phase9-dynamics-atlas.md`
   charter + `phase9-atlas-findings.md`. Taxonomy: identities / weather / flashes
   with half-lives; reversals are vol events, not direction events (+1.9bp ± 3.4
   next-day after 3+red→green); the first build's "+77bp reversal edge" was the
   event day measuring itself (clock rule now in the charter); direction cells
   decompose into beta×bull-window arithmetic + door 6's crisis/low-vol effect.
6. **Phase 9b — graduation test of the atlas's one candidate — FAIL** (`87ce4e7`):
   quiet vol expansion (large, volT_up, volmT_dn): observed t=3.45 vs family-max
   permutation null 95pct 3.58 (selection-adjusted p=0.073) + era-unstable →
   atlas-noise, closed.
7. **A2 sprint frozen and EXECUTED — 2 of 3 properties EXIST** (`87ce4e7`):
   `phase7a2-preregistration.md` (incl. new A2c), `volume_profile_daily` built from
   bars_1m_raw (1,151 days, ~4min), `phase7-findings.md` §A2. **A2c avg dollar trade
   size: residual rank-AC +0.72 (t+1) / +0.56 (t+21), 5/5 eras — PASS. A2b Hill tail
   shape: ~+0.28/month both tails, 5/5 eras — PASS (beat its weak/none prior).**
   A2a volume-profile HHI: +0.07 < 0.10 floor — FAIL (decorative). Monetization map
   frozen: PASSes route ONLY to a Branch B skew/smile extension.

## Where the project stands tonight

- Direction from price/volume: closed (unchanged, re-confirmed twice today by 9b and
  the atlas clock demonstration).
- The dataset's validated persistent invariants are now **three**: volatility
  (monthly weather), trade-size fingerprint (identity, AC 0.72), tail shape
  (identity, AC ~0.28). None monetize in delta-one; all three sharpen Branch B.
- Free queue: fundamentals and dividend/split event clocks remain untested (queued);
  everything else measured or closed.
- **The single open expectancy door is Branch B**, now with more content than when
  FINAL_REPORT wrote it down: see `branchB-plan.md`.

## Data-contract facts verified today (for future scripts)

`ret_1d` = entry(10:00 D)→close(D+1), overlapping, non-compoundable (use
entry-price ratios); `atr_14d` is in dollars; `entry_offset` is a String;
daily_observation has `eod_day_close` (not close_price); polars float NaN passes
`is_not_null()` (bit twice today — use `is_not_nan()`); the +0.0076 IC figure is
full-ML−informed, +0.036 is full-ML−persistence.
